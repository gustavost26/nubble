import React from 'react';

import {Screen, Text} from '@components';

export function MyProfileScreen() {
  return (
    <Screen>
      <Text preset="headingSmall">New MyProfile Screen</Text>
    </Screen>
  );
}
